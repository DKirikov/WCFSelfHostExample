Visual Studio 2013

This example shows how to create a self-hosted service.

It contains two projects: WCFClient1 & SelfHost.
SelfHost is a self-hosted service. It places itself at http://localhost:8080/selfTest.
WCFClient1 creates connection and call some APIs.